<script>
	class ThemeToggle extends HTMLElement {
		constructor() {
			super();
			const button = this.querySelector("button") as HTMLButtonElement;

			function setTheme(dark: boolean) {
				// This could/should be a CustomEvent()
				document.documentElement.classList.toggle("dark", dark);
				// set the aria-pressed attribute, dark mode === aria-pressed=true
				button.setAttribute("aria-pressed", String(dark));
			}

			// button clicked, toggle dark mode
			button.addEventListener("click", () => {
          // set localstorage?
        console.log("setting Localstorage theme", !this.isDark())
        localStorage.setItem("theme", !this.isDark() ? "dark" : "light");
        setTheme(!this.isDark())
      });

			// Initalise button state
			setTheme(this.isDark());
		}

		// returns if the root element contains the dark class
		isDark() {
			return document.documentElement.classList.contains("dark");
		}
	}

	customElements.define("theme-toggle", ThemeToggle);
</script>

<theme-toggle class="ml-auto">
	<button
		type="button"
		id="toggle-theme"
		class="theme-btn group"
		aria-label="Toggle Terminal Mode"
	>
		<!-- Green Terminal (Dark Mode) -->
		<span class="theme-label green-mode" aria-hidden="true">
			<span class="bracket">[</span>
			<span class="mode-text">GREEN</span>
			<span class="bracket">]</span>
		</span>
		<!-- Amber Terminal (Light Mode) -->
		<span class="theme-label amber-mode" aria-hidden="true">
			<span class="bracket">[</span>
			<span class="mode-text">AMBER</span>
			<span class="bracket">]</span>
		</span>
	</button>
</theme-toggle>

<style>
	.theme-btn {
		display: flex;
		align-items: center;
		padding: 0.25rem 0.5rem;
		background: transparent;
		border: 1px solid var(--theme-text);
		color: var(--theme-text);
		font-family: 'JetBrains Mono', monospace;
		font-size: 0.75rem;
		cursor: pointer;
		transition: all 0.2s ease;
	}

	.theme-btn:hover {
		background: rgba(0, 255, 135, 0.1);
		border-color: var(--theme-accent);
		color: var(--theme-accent);
		text-shadow: 0 0 5px var(--theme-accent);
	}

	.theme-label {
		display: flex;
		align-items: center;
	}

	.bracket {
		color: var(--theme-quote);
	}

	.mode-text {
		padding: 0 0.25rem;
	}

	/* Default state (amber/light mode): show GREEN as the current mode indicator */
	.green-mode {
		display: none;
	}

	.amber-mode {
		display: flex;
	}

	/* When dark mode is active: show GREEN as the current mode indicator */
	:global(.dark) .green-mode {
		display: flex;
	}

	:global(.dark) .amber-mode {
		display: none;
	}
</style>

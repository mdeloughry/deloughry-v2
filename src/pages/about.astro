---
import PageLayout from "@/layouts/Base";
import aboutImg from "../assets/Matt.jpg";
import { Image } from "astro:assets";
const meta = {
	title: "About",
	description: "All about me Matt - Developer, Streamer, and Nerd from Sheffield",
};
export const prerender = true;
---

<PageLayout meta={meta}>
	<article class="terminal-about">
		<!-- Command Header -->
		<div class="section-header" aria-hidden="true">
			<span class="prompt">$</span>
			<span class="command">cat ~/about.txt</span>
		</div>

		<!-- Terminal Window with Photo -->
		<header class="about-header">
			<div class="terminal-window">
				<div class="terminal-header" aria-hidden="true">
					<div class="terminal-dots">
						<span class="terminal-dot red"></span>
						<span class="terminal-dot yellow"></span>
						<span class="terminal-dot green"></span>
					</div>
					<span class="terminal-title">~/profile.img</span>
				</div>
				<div class="terminal-content image-container">
					<Image
						src={aboutImg}
						height={aboutImg.height}
						width={aboutImg.width}
						alt="A Photo of Matthew Deloughry looking off to the right."
						loading="eager"
						class="profile-photo"
						transition:name={"profile-image"}
					/>
				</div>
			</div>

			<div class="about-info">
				<h1 class="about-title glow">
					<span class="title-prefix" aria-hidden="true"># </span>ABOUT<span class="cursor-inline" aria-hidden="true"></span>
				</h1>
				<p class="about-subtitle">
					<span class="comment" aria-hidden="true">// </span>Developer, Streamer, and all-round Nerd from Sheffield
				</p>

				<div class="user-details">
					<div class="detail-line"><span class="key">name:</span> <span class="value">"Matthew Peck-Deloughry"</span>,</div>
					<div class="detail-line"><span class="key">alias:</span> <span class="value">"dr_dinomight"</span>,</div>
					<div class="detail-line"><span class="key">location:</span> <span class="value">"Sheffield, UK"</span>,</div>
					<div class="detail-line"><span class="key">role:</span> <span class="value">"Full Stack Developer"</span>,</div>
					<div class="detail-line"><span class="key">experience:</span> <span class="value number">15</span>, <span class="comment">// years (ish)</span></div>
				</div>
			</div>
		</header>

		<!-- Content Sections -->
		<div class="about-content">
			<section class="content-section" aria-labelledby="intro-heading">
				<h2 id="intro-heading" class="sr-only">Introduction</h2>
				<div class="section-cmd" aria-hidden="true">
					<span class="prompt">$</span>
					<span class="command">cat ~/bio/intro.txt</span>
				</div>
				<div class="content-block">
					<p>
						Aye up, I'm Matt<span class="strikethrough">hew</span> Peck-Deloughry, a web developer from Sheffield. I've been
						slinging code for nigh on 15 year now and I can tell you, I've developed a reet fondness for it.
					</p>
					<p>
						You'll mostly find me professionally using PHP and Laravel, but I like to have a bit of fun
						with JavaScript, TypeScript, and Deno too. There's nowt I love more than creating a problem
						and then figuring out how to fix it with a bit of code. It's like a reet good puzzle!
					</p>
				</div>
			</section>

			<section class="content-section" aria-labelledby="experience-heading">
				<h2 id="experience-heading" class="sr-only">Experience</h2>
				<div class="section-cmd" aria-hidden="true">
					<span class="prompt">$</span>
					<span class="command">cat ~/bio/experience.txt</span>
				</div>
				<div class="content-block">
					<p>
						I've worked on all sorts of projects in my time, big and small, and I always approach each one
						with the same level of excitement. I'm always looking to learn new things and improve my
						skills, and I love nothing more than bringing ideas to life through code.
					</p>
					<p>
						But it's not just about the code for me. It's about making things, having a bit of fun, and
						always looking for new challenges.
					</p>
				</div>
			</section>

			<section class="content-section" aria-labelledby="streaming-heading">
				<h2 id="streaming-heading" class="sr-only">Streaming</h2>
				<div class="section-cmd" aria-hidden="true">
					<span class="prompt">$</span>
					<span class="command">cat ~/bio/streaming.txt</span>
				</div>
				<div class="content-block">
					<p>
						Oh, and if you're up for it, you can catch me streaming my coding adventures on Twitch. It's a
						bit of a hoot, if I do say so meself!
					</p>
					<p class="disclaimer">
						<span class="comment">/* AI_GENERATED: The above was generated by AI, asking to write a blurb for sheffield coder and nerd, born and bred! */</span>
					</p>
				</div>
			</section>

			<section class="content-section" aria-labelledby="philosophy-heading">
				<h2 id="philosophy-heading" class="sr-only">Philosophy</h2>
				<div class="section-cmd" aria-hidden="true">
					<span class="prompt">$</span>
					<span class="command">cat ~/bio/philosophy.txt</span>
				</div>
				<div class="content-block">
					<p>
						But really I think this sums me up perfectly, I love the challenge of using some language I
						love to solve something, finding the pit-falls and ways out of those said holes. Finding
						solutions to problems is what it's all about - using all my "hacks" and "lessons" to produce a
						comprehensive solution.
					</p>
					<p>
						I try to share that enthusiasm in everything I do, from mentoring and streaming to just
						chatting with people. When you love what you do, it makes everything more fun and rewarding.
					</p>
				</div>
			</section>

			<!-- Quote Section -->
			<section class="quote-section" aria-labelledby="motto-heading">
				<h2 id="motto-heading" class="sr-only">My Motto</h2>
				<div class="section-cmd" aria-hidden="true">
					<span class="prompt">$</span>
					<span class="command">echo $MOTTO</span>
				</div>
				<blockquote class="terminal-quote">
					<p class="quote-text glow">
						"Av' fun developing, Av' fun learning, then Av'a crack at engineering it!"
					</p>
				</blockquote>
			</section>

			<!-- Skills Section -->
			<section class="skills-section" aria-labelledby="skills-heading">
				<h2 id="skills-heading" class="sr-only">Skills and Technologies</h2>
				<div class="section-cmd" aria-hidden="true">
					<span class="prompt">$</span>
					<span class="command">cat ~/skills.json</span>
				</div>
				<div class="skills-block">
					<pre class="skills-json" aria-label="Skills: Languages include PHP, JavaScript, TypeScript, Go. Frameworks include Laravel, Astro, React, Vue. Tools include Docker, Git, Linux, VS Code. Interests include Open Source, Streaming, Gaming, Learning.">{`{
  "languages": ["PHP", "JavaScript", "TypeScript", "Go"],
  "frameworks": ["Laravel", "Astro", "React", "Vue"],
  "tools": ["Docker", "Git", "Linux", "VS Code"],
  "interests": ["Open Source", "Streaming", "Gaming", "Learning"]
}`}</pre>
				</div>
			</section>
		</div>
	</article>
</PageLayout>

<style>
	.terminal-about {
		font-family: 'JetBrains Mono', monospace;
	}

	.section-header,
	.section-cmd {
		display: flex;
		gap: 0.5rem;
		margin-bottom: 1rem;
	}

	.prompt {
		color: var(--theme-accent);
	}

	.command {
		color: var(--theme-link);
	}

	/* Header Section */
	.about-header {
		display: grid;
		grid-template-columns: auto 1fr;
		gap: 2rem;
		margin-bottom: 3rem;
		align-items: start;
	}

	.terminal-window {
		border: 1px solid var(--theme-text);
		max-width: 280px;
	}

	.terminal-header {
		display: flex;
		align-items: center;
		gap: 1rem;
		padding: 0.5rem 1rem;
		background: rgba(0, 255, 135, 0.05);
		border-bottom: 1px solid var(--theme-text);
	}

	.terminal-dots {
		display: flex;
		gap: 0.5rem;
	}

	.terminal-dot {
		width: 10px;
		height: 10px;
		border-radius: 50%;
	}

	.terminal-dot.red { background: #FF4757; box-shadow: 0 0 5px #FF4757; }
	.terminal-dot.yellow { background: #DAFF01; box-shadow: 0 0 5px #DAFF01; }
	.terminal-dot.green { background: #00FF87; box-shadow: 0 0 5px #00FF87; }

	.terminal-title {
		flex: 1;
		text-align: center;
		color: var(--theme-quote);
		font-size: 0.75rem;
	}

	.terminal-content {
		padding: 0.5rem;
	}

	.image-container {
		display: flex;
		justify-content: center;
	}

	.profile-photo {
		width: 100%;
		height: auto;
		filter: grayscale(100%) contrast(1.1);
		transition: filter 0.3s ease;
	}

	.profile-photo:hover {
		filter: grayscale(0%) contrast(1);
	}

	.about-info {
		display: flex;
		flex-direction: column;
		gap: 1rem;
	}

	.about-title {
		font-size: 2.5rem;
		color: var(--theme-accent);
		margin: 0;
		display: flex;
		align-items: center;
	}

	.title-prefix {
		color: var(--theme-quote);
	}

	.about-subtitle {
		margin: 0;
		font-size: 0.875rem;
	}

	.comment {
		color: var(--theme-quote);
	}

	.user-details {
		padding: 1rem;
		border: 1px dashed var(--theme-text);
		background: rgba(0, 255, 135, 0.02);
	}

	.detail-line {
		margin: 0.25rem 0;
		font-size: 0.875rem;
	}

	.key {
		color: var(--theme-link);
	}

	.value {
		color: var(--theme-text);
	}

	.value.number {
		color: var(--theme-accent);
	}

	.glow {
		text-shadow: 0 0 5px var(--theme-accent), 0 0 10px var(--theme-accent);
	}

	/* Content Sections */
	.about-content {
		display: flex;
		flex-direction: column;
		gap: 2rem;
	}

	.content-section {
		border-left: 2px solid var(--theme-text);
		padding-left: 1rem;
	}

	.content-block {
		padding: 1rem;
		border: 1px dotted var(--theme-text);
		background: rgba(0, 255, 135, 0.02);
	}

	.content-block p {
		margin: 0 0 1rem 0;
		line-height: 1.7;
	}

	.content-block p:last-child {
		margin-bottom: 0;
	}

	.strikethrough {
		text-decoration: line-through;
		color: var(--theme-quote);
	}

	.disclaimer {
		font-size: 0.75rem;
		margin-top: 1rem;
		padding-top: 1rem;
		border-top: 1px dashed var(--theme-quote);
	}

	/* Quote Section */
	.quote-section {
		margin: 2rem 0;
	}

	.terminal-quote {
		margin: 0;
		padding: 1.5rem;
		border: 2px solid var(--theme-accent);
		background: rgba(218, 255, 1, 0.05);
		text-align: center;
	}

	.quote-text {
		margin: 0;
		font-size: 1.25rem;
		font-style: italic;
		color: var(--theme-accent);
	}

	/* Skills Section */
	.skills-section {
		margin-top: 2rem;
	}

	.skills-block {
		border: 1px solid var(--theme-text);
		overflow-x: auto;
	}

	.skills-json {
		margin: 0;
		padding: 1rem;
		font-size: 0.875rem;
		color: var(--theme-text);
		background: rgba(0, 255, 135, 0.02);
	}

	/* Responsive */
	@media (max-width: 768px) {
		.about-header {
			grid-template-columns: 1fr;
			text-align: center;
		}

		.terminal-window {
			max-width: 100%;
			width: 200px;
			margin: 0 auto;
		}

		.about-title {
			font-size: 2rem;
			justify-content: center;
		}

		.user-details {
			text-align: left;
		}

		.content-section {
			padding-left: 0.5rem;
		}

		.quote-text {
			font-size: 1rem;
		}
	}
</style>

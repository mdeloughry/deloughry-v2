---
import PageLayout from "@/layouts/Base";
import aboutImg from "../assets/Matt.jpg";
import { Image } from "astro:assets";
const meta = {
  title: "About",
  description: "All about me Matt - Developer, Gamer, and Nerd from Sheffield",
};
export const prerender = true;
---

<PageLayout meta={meta}>
  <article class="about-page">
    <!-- Header -->
    <header class="about-header">
      <div class="header-content">
        <h1 class="page-title"><span class="accent-slash">//</span> About</h1>
        <p class="subtitle">Developer / Gamer / Sheffield</p>
      </div>
      <div class="about-image-container crt-filter">
        <Image
          src={aboutImg}
          height={aboutImg.height}
          width={aboutImg.width}
          alt="A Photo of Matthew Deloughry"
          loading="eager"
          class="about-image crt-image"
          transition:name="profile-image"
        />
      </div>
    </header>

    <!-- Content sections -->
    <div class="content-grid">
      <section class="content-section">
        <h2 class="section-heading text-accent">Hello</h2>
        <p>
          Ow'do. I'm Matt<span class="strikethrough">hew</span> Peck-Deloughry. I'm
          from Sheffield and I've been doing web dev for about 15 years now. Mostly
          PHP and Laravel &mdash; that's where I earn me keep &mdash; but I'll have
          a go at <strong>owt</strong>(anything really). TypeScript, Deno,
          whatever. If it solves the problem, I'm not fussy.
        </p>
        <p>
          You might know me as <strong>mdeloughry</strong> or
          <strong>Dr_DinoMight</strong>. The second one's from when from
          lockdown boredom... never went away.
        </p>
      </section>

      <section class="content-section">
        <h2 class="section-heading text-info">What I Do</h2>
        <p>
          I like the messy stuff. Untangling some legacy code that nobody wants
          to touch, or taking a vague idea and turning it into something that
          actually works. I've done agency work, platform stuff, big projects,
          little projects and love it all, but for my personal project I'm
          always letting the giddy nerd at heart take over.
        </p>
        <p>
          I do a bit of mentoring, stream sometimes (not as much these days with
          a lil'engineer ruling the roost now), and I try to give back to open
          source where I can. Side projects are a bit of an addiction, honestly.
        </p>
      </section>

      <section class="content-section">
        <h2 class="section-heading text-purple">Away From t' Keyboard</h2>
        <p>
          I play a lot of games. Probably too many. I cook &mdash; badly at
          first, but I'm getting there. And I live near the Peak District so
          I've no excuse not to go for a walk every now and then, even if I do
          spend half of it thinking about code.
        </p>
      </section>

      <section class="content-section">
        <h2 class="section-heading text-pink">This Site</h2>
        <p>
          This is built wi' Astro and Tailwind. It's where I write things up,
          dump projects, and generally have a little spot on the internet that's
          mine.
        </p>
        <p>
          If you want to get in touch, there's links on the home page. I'm
          always happy to chat &mdash; about code, games, Sheffield, whatever
          really.
        </p>
      </section>
    </div>

    <!-- Motto quote -->
    <blockquote class="motto-block">
      <span class="quote-mark">"</span>Av' fun developing, Av' fun learning,
      then Av'a crack at engineering it!<span class="quote-mark">"</span>
    </blockquote>
  </article>

  <style>
    .about-page {
      display: flex;
      flex-direction: column;
      gap: var(--space-2xl);
    }

    /* Header */
    .about-header {
      display: grid;
      grid-template-columns: 1fr auto;
      gap: var(--space-xl);
      align-items: start;
      padding-bottom: var(--space-xl);
      border-bottom: 1px solid rgba(255, 255, 255, 0.08);
    }

    .header-content {
      display: flex;
      flex-direction: column;
      gap: var(--space-xs);
    }

    /* Page title styles from global.css */

    .subtitle {
      font-size: var(--text-base);
      color: var(--theme-muted);
      margin: 0;
    }

    /* Image */
    .about-image-container {
      position: relative;
      width: 240px;
      height: 240px;
      flex-shrink: 0;
    }

    .about-image {
      width: 100%;
      height: 100%;
      object-fit: cover;
      border-radius: var(--radius-subtle);
      filter: grayscale(100%) contrast(1.05);
      transition: filter var(--transition-slow);
    }

    .about-image-container:hover .about-image {
      filter: grayscale(0%) contrast(1);
    }

    /* Content sections */
    .content-grid {
      display: grid;
      grid-template-columns: repeat(2, 1fr);
      gap: var(--space-xl);
    }

    .content-section {
      display: flex;
      flex-direction: column;
      gap: var(--space-sm);
    }

    .section-heading {
      font-family: "JetBrains Mono", monospace;
      font-size: var(--text-lg);
      font-weight: 600;
      margin: 0;
      letter-spacing: -0.01em;
    }

    .content-section p {
      font-size: var(--text-base);
      line-height: 1.8;
      color: var(--theme-muted);
      margin: 0;
    }

    .strikethrough {
      text-decoration: line-through;
      opacity: 0.5;
    }

    .disclaimer {
      font-size: var(--text-sm) !important;
      padding: var(--space-sm);
      border-radius: var(--radius-subtle);
      background: rgba(255, 255, 255, 0.03);
      font-style: italic;
    }

    /* Motto block */
    .motto-block {
      margin: 0;
      padding: var(--space-xl);
      border: var(--border-weight) solid var(--theme-accent);
      border-radius: var(--radius-subtle);
      font-family: "JetBrains Mono", monospace;
      font-size: var(--text-lg);
      color: var(--theme-text);
      text-align: center;
      background: var(--theme-bg-secondary);
    }

    .quote-mark {
      color: var(--theme-accent);
      font-size: 1.5em;
      vertical-align: -0.1em;
    }

    /* Responsive */
    @media (max-width: 900px) {
      .about-header {
        grid-template-columns: 1fr;
        text-align: center;
      }

      .about-image-container {
        margin: 0 auto;
        width: 180px;
        height: 180px;
      }

      .content-grid {
        grid-template-columns: 1fr;
      }
    }
  </style>
</PageLayout>
